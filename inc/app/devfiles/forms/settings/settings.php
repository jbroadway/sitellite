; <?php /*

[Form]

action	= devfiles-settings-form

[files]

alt				= Allow new files
type			= select

[files_date]

alt				= Turn files off on
type			= date

[contact]

alt				= "Notify the following address when new files are added (separate multiple addresses with a comma)"
type			= textarea
rows			= 2
cols			= 30
labelPosition	= left

[ignore_list]

alt				= "Do not send notices when files are added by the following users (separate with a comma)"
type			= textarea
rows			= 2
cols			= 30
labelPosition	= left

[allowed_types]

alt				= "Explicitly allow only files with the following extensions (separate with a comma)"
type			= textarea
rows			= 2
cols			= 30
labelPosition	= left

[not_allowed]

alt				= "Disallow files with the following extensions (separate with a comma)"
type			= textarea
rows			= 2
cols			= 30
labelPosition	= left

[submit_button]

type			= submit

; */ ?>