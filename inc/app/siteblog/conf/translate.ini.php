; <?php /*

; List of database tables and the specified field that should be indexed
; by the translation indexer.  These can then be inserted into application
; templates via {filter intl_get}{field_name}{end filter} instead of
; just {field_name} as you usually would.

siteblog_category = title

; */ ?>